# Processed data

Store reproducibly derived datasets locally in this directory. Every processed dataset should be traceable to raw inputs and code.

Processed datasets are ignored by Git by default.
